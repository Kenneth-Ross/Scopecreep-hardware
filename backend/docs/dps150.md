# DPS-150 Power Supply Backend

Python FastAPI backend for controlling the FNIRSI DPS-150 DC power supply over USB serial.

---

## System Architecture

```mermaid
graph TD
    subgraph Client
        Plugin[JetBrains Plugin / CLI Script]
    end

    subgraph Backend ["Python Backend (FastAPI :8000)"]
        API[PSU Routes\n/psu/*]
        Driver[DPS150 Driver]
        Packets[Packet Layer\nbuild_packet / _parse_response]
    end

    subgraph Hardware
        USB[USB Serial\n/dev/cu.usbmodem* macOS\n/dev/ttyACM0 Linux]
        PSU[FNIRSI DPS-150\n0–30V / 0–5A]
    end

    Plugin -->|HTTP JSON| API
    API --> Driver
    Driver --> Packets
    Packets -->|115200 baud\nrtscts=True| USB
    USB --> PSU
```

---

## Serial Protocol

```mermaid
sequenceDiagram
    participant Host
    participant DPS150

    Host->>DPS150: F1 C1 00 01 01 02 (session open / keyboard-lock)
    Host->>DPS150: F1 B0 00 01 05 06 (confirm baud 115200)

    Note over Host,DPS150: Device ready — 50ms between commands

    Host->>DPS150: SET voltage (F1 B1 C1 04 [float32 LE] [cksum])
    Host->>DPS150: SET current limit (F1 B1 C2 04 [float32 LE] [cksum])
    Host->>DPS150: SET output ON (F1 B1 DB 01 01 DD)

    DPS150-->>Host: Telemetry broadcast ~500ms (unsolicited)
    DPS150-->>Host: F0 A1 C3 0C [V][I][P] (measurements)

    Host->>DPS150: GET measurements (F1 A1 C3 00 C3)
    DPS150-->>Host: F0 A1 C3 0C [voltage][current][power]

    Host->>DPS150: SET output OFF (F1 B1 DB 01 00 DC)
    Host->>DPS150: F1 C1 00 01 00 01 (session close)
```

### Frame Format

```
[0xF1] [cmd] [register] [len] [payload…] [checksum]
  │      │       │         │       │           │
  │      │       │         │       │           └─ (register + len + sum(payload)) % 256
  │      │       │         │       └─ data bytes (floats = IEEE-754 LE 32-bit)
  │      │       │         └─ payload length
  │      │       └─ register address
  │      └─ 0xB1 SET | 0xA1 GET
  └─ 0xF1 host→device | 0xF0 device→host
```

### Key Registers

| Register | Hex | Direction | Type | Description |
|---|---|---|---|---|
| VOLTAGE_SET | 0xC1 | SET | float32 | Output voltage setpoint (V) |
| CURRENT_SET | 0xC2 | SET | float32 | Current limit (A) |
| OUTPUT_ENABLE | 0xDB | SET | uint8 | 0=off, 1=on |
| MEASUREMENTS | 0xC3 | GET | 3×float32 | Output V, I, P |
| INPUT_VOLTAGE | 0xC0 | GET | float32 | USB input voltage |
| TEMPERATURE | 0xC4 | GET | float32 | Internal temperature |
| ALL_STATE | 0xFF | GET | 139 bytes | Full device state dump |

---

## Driver Architecture

```mermaid
classDiagram
    class InstrumentDriver {
        <<abstract>>
        +connect() None
        +disconnect() None
        +is_connected bool
    }

    class DPS150 {
        -_port str
        -_baud int
        -_timeout float
        -_ser Serial
        +connect() None
        +disconnect() None
        +is_connected bool
        +set_voltage(volts) None
        +set_current(amps) None
        +enable_output(on) None
        +get_measurements() dict
        +get_all_state() dict
        -_send(cmd, register, payload) None
        -_read_response() tuple
    }

    class PacketLayer {
        +build_packet(cmd, register, payload) bytes
        +_parse_response(data) tuple
        +INIT_PACKET bytes
    }

    InstrumentDriver <|-- DPS150
    DPS150 --> PacketLayer
```

---

## API Endpoints

```mermaid
graph LR
    subgraph PSU ["/psu/*"]
        C[POST /connect\nbody: port]
        D[DELETE /disconnect]
        M[GET /measurements\nreturns V·I·P]
        S[GET /state\nreturns full device state]
        V[POST /voltage\nbody: volts]
        A[POST /current\nbody: amps]
        O[POST /output\nbody: enabled]
    end

    H[GET /health]
```

### Request / Response Examples

**Connect**
```json
POST /psu/connect
{"port": "/dev/cu.usbmodem1480DFDC40251"}
→ {"status": "connected", "port": "/dev/cu.usbmodem1480DFDC40251"}
```

**Set voltage**
```json
POST /psu/voltage
{"volts": 5.0}
→ {"status": "ok", "voltage_set": 5.0}
```

**Set current limit**
```json
POST /psu/current
{"amps": 0.2}
→ {"status": "ok", "current_set": 0.2}
```

**Enable output**
```json
POST /psu/output
{"enabled": true}
→ {"status": "ok", "output_enabled": true}
```

**Read measurements**
```json
GET /psu/measurements
→ {"voltage": 4.982, "current": 0.1523, "power": 0.7591}
```

**Full state**
```json
GET /psu/state
→ {
    "input_voltage": 12.1,
    "voltage_set": 5.0,
    "current_set": 0.2,
    "voltage": 4.982,
    "current": 0.1523,
    "power": 0.7591,
    "temperature": 31.4,
    "output_enabled": true,
    "protection": 0,
    "mode": "CV",
    "voltage_max": 30.0,
    "current_max": 5.0
  }
```

**Error — not connected**
```json
→ 503 {"detail": "PSU not connected"}
```

**Error — serial port unavailable**
```json
→ 503 {"detail": "Cannot open port /dev/cu.usbmodem...: [Errno 16] Resource busy"}
```

---

## Hardware Setup

```mermaid
graph LR
    Mac[macOS Laptop] -->|USB-A to Micro-USB\ndata cable required| PSU[FNIRSI DPS-150]
    PSU -->|output terminals| Load[Load / Multimeter]

    subgraph macOS
        Port["/dev/cu.usbmodem*"]
    end

    subgraph Linux
        Port2["/dev/ttyACM0"]
    end

    Mac --> Port
```

**macOS notes:**
- Port appears as `/dev/cu.usbmodem*` (use `cu.` not `tty.` variant)
- Requires a data cable — charge-only cables carry no serial data
- Hardware flow control (`rtscts=True`) is required — device won't respond without it
- No additional driver needed (AT32 VCP enumerates natively on macOS)

**Linux notes:**
- Port appears as `/dev/ttyACM0`
- Add user to `dialout` group if permission denied: `sudo usermod -aG dialout $USER`

---

## Running

```bash
cd backend

# Install dependencies
pip install -r requirements.txt

# Start backend (default port 8000)
python main.py

# Override serial port via env var
PSU_PORT=/dev/cu.usbmodem1480DFDC40251 python main.py
```

**Environment variables:**

| Variable | Default | Description |
|---|---|---|
| `PSU_BAUD` | `115200` | Serial baud rate |
| `PSU_TIMEOUT` | `0.5` | Serial read timeout (seconds) |

---

## Testing

```bash
cd backend

# Unit + API tests (no hardware required)
python -m pytest tests/ -v

# Hardware validation — set 5V / 200mA, read measurements
python set_voltage.py /dev/cu.usbmodem1480DFDC40251 5.0 0.2

# Hardware validation — with known load resistance
# Script calculates expected current and operating mode (CC vs CV)
python set_voltage.py /dev/cu.usbmodem1480DFDC40251 5.0 0.2 33
```

---

## CC vs CV Mode

```mermaid
graph TD
    Start([Output enabled]) --> Q{Is load current\n≥ current limit?}
    Q -->|No| CV[CV mode\nVoltage regulated\nCurrent < limit]
    Q -->|Yes| CC[CC mode\nCurrent clamped at limit\nVoltage sags]
    CV -->|Load increases| Q
    CC -->|Load decreases| Q
```

- **CV (Constant Voltage):** normal operation — voltage held at setpoint, current free to vary below limit
- **CC (Constant Current):** overload — current clamped at limit, voltage drops. Triggered by short circuit or low-resistance load.

---

## Known Quirks

- **`rtscts=True` required** — hardware flow control must be enabled or the device ignores all commands
- **50ms between commands** — device needs inter-command delay; sending too fast drops commands silently
- **SET commands are fire-and-forget** — no ACK from device; verify via GET or telemetry
- **Session open required first** — sending the INIT_PACKET (`F1 C1 00 01 01 02`) before any other command is mandatory; device ignores commands without it
- **Use `cu.` not `tty.` on macOS** — `tty.*` devices block on open until a carrier is detected; `cu.*` opens immediately
